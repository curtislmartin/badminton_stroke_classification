# --- ML & Data ---
torch
torchvision
numpy
pandas
matplotlib
scikit-learn
positional-encodings  # Positional encoding layers used by BST transformer model (bst_refactor/stroke_classification/model/)
torchinfo             # Model summary utility used by tempose.py in the BST model module
tensorboard           # Backs torch.utils.tensorboard.SummaryWriter (imported at module load in bst_train.py)
torcheval             # multiclass_f1_score used in the bst_train training loop
transformers          # HuggingFace; provides get_cosine_schedule_with_warmup for the bst_train LR schedule
pyyaml                # tests/test_active_classes.py reads taxonomy YAMLs


# --- Backend API ---
fastapi
uvicorn  # The server that runs FastAPI
python-multipart  # Required for file upload endpoints

# --- Testing & Infrastructure ---
pytest
httpx  # Required by FastAPI TestClient
mlflow  # Experiment tracking

# bst_refactor subproject has complex dependencies; so requires 2-3 separate venvs depending on how you run it.
# req Python 3.11.x. Probably 3.11.15 should be fine (to test soon).